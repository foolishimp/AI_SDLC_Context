# Test requirements for AI_SDLC_Context

# Core testing framework
pytest>=7.0.0

# Test coverage
pytest-cov>=4.0.0

# Mocking and fixtures
pytest-mock>=3.10.0

# Additional useful pytest plugins
pytest-xdist>=3.0.0  # Run tests in parallel with -n flag
pytest-timeout>=2.1.0  # Add timeouts to tests

# For testing async code (if needed in future)
pytest-asyncio>=0.21.0
