# Implements: REQ-F-TESTING-001 (Test coverage validation)
# Implements: REQ-NFR-COVERAGE-001 (Test coverage minimum 80%)

[pytest]
# Pytest configuration for ai_sdlc_method MCP service

# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
    --color=yes

# Coverage options (if using pytest-cov)
# Uncomment to enable coverage reporting
# addopts =
#     --cov=src/ai_sdlc_config
#     --cov-report=html
#     --cov-report=term-missing

# Markers for categorizing tests
markers =
    unit: Unit tests for individual components
    integration: Integration tests across multiple components
    slow: Tests that take longer to run

# Ignore patterns
norecursedirs = .git .tox dist build *.egg examples

# Minimum Python version
minversion = 3.8
